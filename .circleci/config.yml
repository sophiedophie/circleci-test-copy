
version: 2.1

base_image: &base_image
  circleci/ruby:2.4-node

default_config: &default_config
  docker:
    - image: *base_image

jobs:
  build:
    <<: *default_config

    steps:
      - checkout
      - run: echo ${CIRCLECI_PULL_REQUEST}


workflows:
  test-env:
    jobs:
      - build