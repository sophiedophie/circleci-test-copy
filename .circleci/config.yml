version: 2.1
workflows:
  version: 2
  nightly:
    triggers:
    - schedule:
        cron: 33 7 * * *
        filters:
          branches:
            only:
            - master
    jobs:
    - test
jobs:
  test:
    docker:
      - image: circleci/node:11.1.0
    working_directory: ~/repo
    steps:
      - checkout
      - run:
          name: store env and echo
          command: |
            echo "hello world"

          