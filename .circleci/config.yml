version: 2.1
jobs:
  build:
    docker:
      - image: circleci/node:lts
    steps:
      - run:
          name: source BASH_ENV
          command: source $BASH_ENV
workflows:
  workflow:
    jobs:
      - build