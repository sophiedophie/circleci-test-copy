version: 2.1

orbs:
  win: circleci/windows@2.2.0

jobs:
  build:
    executor:
      name: win/default
      shell: powershell.exe
      size: large
    steps:
      - checkout
      - run:
          name: check dismx
          command: |
            Dism /online /Get-Features