version: 2.0
jobs:
  build:
    docker:
      - image: circleci/node:latest
    steps:
      - checkout
      - run: echo "hello world"

workflows:
  version: 2
  custom-workflow:
    jobs:
      - build
