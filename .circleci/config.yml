
version: 2.1

parameters:
  RUBY_VERSION:
    type: string
    default: "2.4"

base_image: &base_image
  circleci/ruby:${RUBY_VERSION}-node

default_config: &default_config
  docker:
    - image: *base_image

jobs:
  build:
    <<: *default_config

    steps:
      - checkout
      - run: echo ${CIRCLECI_PULL_REQUEST}


workflows:
  test-env:
    jobs:
      - build